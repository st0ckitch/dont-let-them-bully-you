{
  "name": "dont-let-them-bully-you",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run check && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "no-any": "node tools/no-any.mjs",
    "test": "cd tools && for t in hextest shoptest determinism nettest aitest regress combattest balance; do echo \"--- $t\" && node \"$t.mjs\" || exit 1; done",
    "check": "npm run typecheck && npm run no-any && npm test"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
